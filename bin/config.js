const {DEFAULT_BASE_MODE, DEFAULT_OUTPUT_PATH, DEFAULT_SERVER_PORT} = require('../constants')

const config = {
    baseMode: DEFAULT_BASE_MODE,
    serverPort: DEFAULT_SERVER_PORT,
    outputPath: DEFAULT_OUTPUT_PATH,
}

module.exports = config
