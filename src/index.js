import turnOn from './turnOn'
import getInjectedFunction from './getInjectedFunction'
import setConfig from './setConfig'

const perfit = {
    setConfig,
    getInjectedFunction,
    turnOn,
}

export default perfit
